---
milestone: v1.1
audited: 2026-03-31T23:47:57Z
status: tech_debt
scores:
  requirements: 6/6
  phases: 2/2
  integration: 2/2
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: validation-hygiene
    items:
      - "04-VALIDATION.md and 05-VALIDATION.md remain draft strategy documents with pending rows, so Nyquist validation hygiene is still partial at milestone close."
      - "05-VALIDATION.md still records wave_0_complete: false even though the milestone blocker was closed afterward."
  - phase: 05-wrapper-runtime-contract
    items:
      - "There is still no explicit automated test proving `ACO_CAO_BASE_URL` wins when both `ACO_CAO_BASE_URL` and `WRAPPER_CAO_BASE_URL` are set."
nyquist:
  compliant_phases: []
  partial_phases:
    - "04-canonical-command-surface"
    - "05-wrapper-runtime-contract"
  missing_phases: []
  overall: partial
---

# v1.1 Milestone Audit

## Summary

`v1.1` achieved its intended product scope after closing one late milestone blocker in `CMD-03`. All 6 milestone requirements are now satisfied, cross-phase wiring between the `aco` command-surface work and the preserved `.wrapper*` runtime contract is intact, and the key end-to-end user flows complete as designed.

The final audit verdict is **`tech_debt`**, not `passed`, because validation hygiene is still partial and one environment-variable preference path remains unproven by explicit automated coverage. Those are non-blocking follow-ups rather than shipment blockers.

## Scope

Included phases:

- Phase 04: Canonical Command Surface
- Phase 05: Wrapper Runtime Contract

Milestone totals:

- 2 phases
- 7 plans
- 6 tasks

## Requirements Coverage

| Requirement | Assigned Phase | Final Status | Evidence |
|-------------|----------------|--------------|----------|
| CMD-01 | Phase 04 | satisfied | `package.json` exposes only `aco`; canonical help/version flows verified in Phase 04 verification and command-surface tests |
| CMD-02 | Phase 04 | satisfied | `aco`-only help, version, unknown-command, and stale-entry remediation verified by Phase 04 tests and verification report |
| WRAP-03 | Phase 04 | satisfied | stale `wrapper` remediation plus owned-shim cleanup verified in Phase 04 verification and install-state cleanup tests |
| CMD-03 | Phase 05 | satisfied | `src/cli.ts` keeps built-ins ahead of aliases; targeted tests prove `help`, `workflow`, and `workflow-run` still dispatch even when reserved alias names exist |
| WRAP-01 | Phase 05 | satisfied | `aco setup` preserves and initializes `.wrapper.json`; verified in `test/setup.test.ts` and Phase 05 verification |
| WRAP-02 | Phase 05 | satisfied | workflow artifacts remain under `.wrapper/workflows`; verified in `test/artifacts.test.ts`, `test/workflow-runner.test.ts`, and workflow CLI coverage |

**Requirements score:** 6/6 satisfied

## Phase Verification Coverage

| Phase | Verification Status | Verdict |
|-------|---------------------|---------|
| Phase 04 | `04-VERIFICATION.md` present, status `passed` | verified |
| Phase 05 | `05-VERIFICATION.md` present and updated after the CMD-03 correction | verified |

**Phase score:** 2/2 verified

## Cross-Phase Integration

### Verified Wiring

- Phase 04's canonical `aco` command surface still owns help, version, unknown-command, and stale-entry recovery behavior.
- Phase 05 preserves `.wrapper.json` and `.wrapper/workflows` as the repo-local contract under that `aco` surface.
- Built-in subcommands now correctly stay ahead of aliases, so Phase 05 no longer undermines the Phase 04 command-surface guarantee.
- Workflow execution continues to resolve roles and artifact paths from the preserved `.wrapper*` contract without reopening the public-command rename.

### Verdict

No broken cross-phase wiring remains after the CMD-03 fix.

**Integration score:** 2/2

## End-to-End Flows

| Flow | Result | Evidence |
|------|--------|----------|
| Canonical `aco` command surface | complete | `test/canonical-command-surface.test.ts` plus Phase 04 verification |
| Repo-local setup through `.wrapper.json` | complete | `test/setup.test.ts` plus Phase 05 verification |
| Workflow/artifact runtime under `.wrapper/workflows` with built-ins-first dispatch | complete | `test/workflow-cli.test.ts`, `test/workflow-runner.test.ts`, `test/artifacts.test.ts` |

**Flow score:** 3/3

## Technical Debt

### Validation / Nyquist Debt

- Both phase `*-VALIDATION.md` files remain draft strategy documents rather than fully closed validation artifacts.
- Phase 05 still shows `wave_0_complete: false`, so Nyquist hygiene is partial even though the functional blocker was fixed.

### Non-Blocking Test Debt

- The runtime fallback logic prefers `ACO_CAO_BASE_URL` over `WRAPPER_CAO_BASE_URL`, but there is not yet an explicit automated test for the dual-env case.
- `test/alias.test.ts` still carries a misleading `setup`-shadowing description for a help-path assertion, so the reserved-alias coverage should be named or extended more clearly in a later cleanup pass.

## Final Verdict

## ⚡ Milestone v1.1 — Tech Debt Review

**Score:** 6/6 requirements satisfied  
**Report:** `.planning/v1.1-MILESTONE-AUDIT.md`

All milestone requirements are satisfied. No critical blockers remain. The shipped milestone is valid, but validation hygiene is still incomplete and one preference-path assertion remains untested.

### Recommended Follow-up

- Keep the current `v1.1` archive and tag as the shipped record
- Backfill stricter validation hygiene later with `$gsd-validate-phase 4` and `$gsd-validate-phase 5` if desired
- Add explicit dual-env coverage for `ACO_CAO_BASE_URL` precedence in the next runtime-focused sweep
