---
phase: 01-foundation-environment-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - dist/cli.js
  - dist/commands/setup.js
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "npm run build exits 0 and dist/cli.js exists"
    - "npm test exits 0 — all setup tests pass"
    - "wrapper setup runs on this machine and prints four checkmark lines ending with 'Setup complete.'"
    - "Running wrapper setup a second time prints 'already exists' and 'already configured' lines"
  artifacts:
    - path: "dist/cli.js"
      provides: "Compiled CLI binary entry point"
    - path: "dist/commands/setup.js"
      provides: "Compiled setupCommand"
  key_links:
    - from: "dist/cli.js"
      to: "dist/commands/setup.js"
      via: "import { setupCommand } from './commands/setup.js'"
      pattern: "setupCommand"
---

<objective>
Compile the TypeScript source, run the test suite, and do a live smoke-test of `wrapper setup` on the real machine.

Purpose: Confirms the Phase 1 implementation is correct end-to-end — types compile, unit tests pass, and the actual command works on the developer's machine.
Output: Green build, green tests, human confirmation of working setup flow.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-environment-setup/01-01-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run tests</name>
  <files>dist/cli.js, dist/commands/setup.js</files>
  <read_first>
    - src/cli.ts — confirm source is as written before building
    - src/commands/setup.ts — confirm source is as written before building
    - test/setup.test.ts — confirm test file exists before running tests
  </read_first>
  <action>
Run the following commands in sequence from the project root. Stop and report on any failure.

Step 1 — TypeScript lint (type check without emitting):
  npm run lint

If lint fails: read the TypeScript error output carefully. Common causes:
  - Missing .js extension on an import in src/commands/setup.ts or src/cli.ts
  - Type mismatch in function signature
  - Using a Node API that requires a type not covered by @types/node ^20.0.0
Fix the source file causing the error and re-run lint before proceeding.

Step 2 — Compile:
  npm run build

If build fails: same diagnosis as lint failure. Check that all deleted files (download.ts,
lockfile.ts, types.ts, index.ts) have no lingering import references in any remaining source file.

Step 3 — Run tests:
  npm test

If tests fail: read the test output. The most likely failure points are:
  - process.env.HOME override not being picked up by homedir() in setup.ts
    (homedir() reads HOME env var on Linux/macOS — setting process.env.HOME before
    calling setupCommand() should work; if not, pass tempHome as a parameter or
    restructure the test to call file-writing logic directly)
  - The dist/ files are stale if source was edited after build — run npm run build again
  - spawnSync('which', [...]) returns different status in test environment — verify tools are still on PATH

All three steps must exit 0 before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && npm run lint && npm run build && npm test && echo "BUILD+TEST PASS"</automated>
  </verify>
  <done>npm run lint exits 0, npm run build exits 0 producing dist/cli.js and dist/commands/setup.js, npm test exits 0 with all test cases passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test wrapper setup on real machine</name>
  <files>dist/cli.js</files>
  <action>
Claude runs these commands and captures their output for the human to review:

  node dist/cli.js setup
  node dist/cli.js setup
  grep -c "ai-cli.conf" ~/.tmux.conf
  cat ~/.config/tmux/ai-cli.conf
  node dist/cli.js help

Present the actual output of each command to the user.
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && node dist/cli.js setup && node dist/cli.js setup && echo "Smoke run complete"</automated>
  </verify>
  <done>Human confirms all five checks pass (see how-to-verify below).</done>
  <what-built>
    Full `wrapper setup` command: prerequisite check (cao, tmux, workmux), writing ~/.config/tmux/ai-cli.conf, injecting source-file line into ~/.tmux.conf. Built from src/ via `npm run build`.
  </what-built>
  <how-to-verify>
    Run these commands and observe the output:

    1. First run — expect all checkmark lines and "Setup complete.":
         node dist/cli.js setup

       Expected output:
         [checkmark] prerequisites: cao, tmux, workmux found
         [checkmark] ~/.config/tmux/ai-cli.conf written   (OR "already exists" if file pre-existed)
         [checkmark] ~/.tmux.conf: source line added       (OR "already configured" if line pre-existed)
         Setup complete.

    2. Second run immediately after — expect idempotency messages:
         node dist/cli.js setup

       Expected output:
         [checkmark] prerequisites: cao, tmux, workmux found
         [checkmark] ~/.config/tmux/ai-cli.conf: already exists
         [checkmark] ~/.tmux.conf: already configured
         Setup complete.

    3. Verify no duplicate source-file lines were added:
         grep -c "ai-cli.conf" ~/.tmux.conf

       Expected output: 1

    4. Verify ai-cli.conf content:
         cat ~/.config/tmux/ai-cli.conf

       Expected output:
         # ai-cli-orch-wrapper tmux config
         # Managed by wrapper setup — do not edit manually.
         # Phase 2 will populate CLI alias bindings here.

    5. Check help output works:
         node dist/cli.js help

       Must print usage block with "setup" listed as a command.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass. Describe any failures if not.</resume-signal>
</task>

</tasks>

<verification>
All four SETUP requirements verified:

SETUP-01: `wrapper setup` runs and exits 0 — confirmed by Task 1 build + Task 2 smoke test
SETUP-02: Idempotent — confirmed by second run in Task 2 showing "already exists" / "already configured"
SETUP-03: Missing prereq exits 1 with correct message — confirmed by unit test in Plan 01 Task 3
SETUP-04: ai-cli.conf written, source-file line injected exactly once — confirmed by Task 2 grep check
</verification>

<success_criteria>
- npm run lint exits 0
- npm run build exits 0
- npm test exits 0
- `node dist/cli.js setup` run twice produces no duplicate source-file entries in ~/.tmux.conf
- `node dist/cli.js help` lists the setup command
- Human approves smoke test results
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-environment-setup/01-02-SUMMARY.md`
</output>
