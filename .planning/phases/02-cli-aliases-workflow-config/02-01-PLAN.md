---
phase: 02-cli-aliases-workflow-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/setup.test.ts
  - test/config.test.ts
  - test/alias.test.ts
autonomous: true
requirements:
  - ALIAS-01
  - ALIAS-02
  - CONFIG-01
  - CONFIG-02

must_haves:
  truths:
    - "test/setup.test.ts no longer asserts the stale Phase 2 placeholder comment"
    - "test/config.test.ts exists and has failing tests for readWrapperConfig"
    - "test/alias.test.ts exists and has failing tests for aliasCommand dispatch"
    - "npm run build && node --test reports failing tests (RED), not a missing-file error"
  artifacts:
    - path: "test/setup.test.ts"
      provides: "Updated assertion — old placeholder text removed"
      contains: "CLI alias bindings are managed via .wrapper.json"
    - path: "test/config.test.ts"
      provides: "Test scaffold for CONFIG-01 (config read, missing file, malformed JSON)"
      exports: []
    - path: "test/alias.test.ts"
      provides: "Test scaffold for ALIAS-01, ALIAS-02, CONFIG-02, unknown-alias error path"
      exports: []
  key_links:
    - from: "test/alias.test.ts"
      to: "dist/commands/alias.js"
      via: "dynamic import"
      pattern: "import.*dist/commands/alias"
    - from: "test/config.test.ts"
      to: "dist/config/wrapper-config.js"
      via: "dynamic import"
      pattern: "import.*dist/config/wrapper-config"
---

<objective>
Create the Wave 0 test scaffold for Phase 2. Failing tests must exist before implementation so the RED→GREEN cycle is honest and the Nyquist constraint is satisfied.

Purpose: Establish the test contracts that implementation tasks (Plan 02, Plan 03) must satisfy. Also fixes the stale comment assertion in test/setup.test.ts that would fail after D-07 is implemented.
Output: Three test files — one updated, two new — all failing with import errors (modules do not exist yet).
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cli-aliases-workflow-config/02-CONTEXT.md
@.planning/phases/02-cli-aliases-workflow-config/02-RESEARCH.md
@.planning/phases/02-cli-aliases-workflow-config/02-VALIDATION.md

<interfaces>
<!-- Existing test pattern from test/setup.test.ts — replicate exactly for new tests -->

<!-- Import style (NodeNext ESM, dynamic import of compiled dist artifact): -->
const { setupCommand } = await import('../dist/commands/setup.js');

<!-- Test runner: Node.js built-in -->
import { test } from 'node:test';
import assert from 'node:assert/strict';

<!-- Temp home pattern for file-system tests: -->
import { mkdtempSync, readFileSync, existsSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { tmpdir } from 'node:os';

function makeTempHome(): string {
  return mkdtempSync(join(tmpdir(), 'wrapper-test-'));
}

<!-- spawnSync pattern for process exit tests: -->
import { spawnSync } from 'node:child_process';
const result = spawnSync(process.execPath, ['dist/cli.js', '<command>'], {
  cwd: join(import.meta.dirname ?? new URL('.', import.meta.url).pathname, '..'),
  encoding: 'utf8',
});
assert.equal(result.status, 1);

<!-- Current stale assertion (line 40 of test/setup.test.ts) that MUST be replaced: -->
assert.ok(content.includes('# Phase 2 will populate CLI alias bindings here.'), 'should include phase 2 comment');

<!-- New assertion after D-07 fix (exact string the updated AI_CLI_CONF_CONTENT must contain): -->
assert.ok(content.includes('# CLI alias bindings are managed via .wrapper.json in the project root.'), 'should include wrapper.json reference comment');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale comment assertion in test/setup.test.ts</name>
  <files>test/setup.test.ts</files>
  <read_first>
    - test/setup.test.ts — read the full file; the stale assertion is at line 40
    - src/commands/setup.ts — read AI_CLI_CONF_CONTENT constant (lines 14-19); the new comment text must match what Plan 03 will write there
  </read_first>
  <action>
Replace line 40 of test/setup.test.ts. The current assertion is:

    assert.ok(content.includes('# Phase 2 will populate CLI alias bindings here.'), 'should include phase 2 comment');

Replace it with:

    assert.ok(content.includes('# CLI alias bindings are managed via .wrapper.json in the project root.'), 'should include wrapper.json reference comment');

Do NOT change anything else in the file. The rest of the assertions (title comment, managed-by comment) remain valid.

Why: AI_CLI_CONF_CONTENT in setup.ts will be updated in Plan 03 (per D-07). The test must match the new text. Updating the test first means it will fail (RED) until Plan 03 implements the change — correct behavior.
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && npm run build && node --test test/setup.test.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test/setup.test.ts line 40 contains the string: `CLI alias bindings are managed via .wrapper.json in the project root.`
    - test/setup.test.ts does NOT contain the string: `Phase 2 will populate CLI alias bindings here`
    - Running `npm run build && node --test test/setup.test.ts` exits non-zero (test 1 fails because setup.ts still has old text — this is correct RED state)
    - Tests 2, 3, 4, 5 in setup.test.ts still pass (only test 1 is expected to fail at this point)
  </acceptance_criteria>
  <done>test/setup.test.ts updated — stale assertion replaced, test now correctly fails RED against current setup.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create test/config.test.ts with failing stubs for CONFIG-01</name>
  <files>test/config.test.ts</files>
  <read_first>
    - test/setup.test.ts — read for file structure, import style, makeTempHome pattern to replicate
    - .planning/phases/02-cli-aliases-workflow-config/02-RESEARCH.md — Pattern 1 (readWrapperConfig) and Pattern 4 (interfaces) define exact function signatures and return shapes being tested
  </read_first>
  <action>
Create test/config.test.ts with the following exact content:

```typescript
/**
 * Wrapper config tests
 *
 * Tests for readWrapperConfig() covering CONFIG-01 (config read, missing file fallback, malformed JSON fallback).
 */

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { tmpdir } from 'node:os';

function makeTempDir(): string {
  return mkdtempSync(join(tmpdir(), 'wrapper-config-test-'));
}

// Test 1: reads aliases and roles from a valid .wrapper.json
test('readWrapperConfig reads aliases and roles from a valid config file', async () => {
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeFileSync(
    configPath,
    JSON.stringify({
      aliases: {
        claude: { provider: 'claude_code', agent: 'developer' },
        gemini: { provider: 'gemini_cli', agent: 'developer' },
      },
      roles: {
        orchestrator: 'claude_code',
        reviewer: 'gemini_cli',
      },
    }),
    'utf8',
  );

  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const config = readWrapperConfig(configPath);

  assert.equal(config.aliases['claude'].provider, 'claude_code');
  assert.equal(config.aliases['claude'].agent, 'developer');
  assert.equal(config.aliases['gemini'].provider, 'gemini_cli');
  assert.equal(config.roles['orchestrator'], 'claude_code');
  assert.equal(config.roles['reviewer'], 'gemini_cli');
});

// Test 2: returns empty defaults when file is missing (graceful fallback)
test('readWrapperConfig returns empty defaults when config file is missing', async () => {
  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const config = readWrapperConfig('/nonexistent/path/.wrapper.json');

  assert.deepEqual(config.aliases, {});
  assert.deepEqual(config.roles, {});
});

// Test 3: returns empty defaults when file contains malformed JSON
test('readWrapperConfig returns empty defaults when JSON is malformed', async () => {
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeFileSync(configPath, '{ this is not valid json }', 'utf8');

  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const config = readWrapperConfig(configPath);

  assert.deepEqual(config.aliases, {});
  assert.deepEqual(config.roles, {});
});

// Test 4: arbitrary provider string passes through without validation (CONFIG-02)
test('readWrapperConfig accepts arbitrary provider strings without validation', async () => {
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeFileSync(
    configPath,
    JSON.stringify({
      aliases: {
        newai: { provider: 'some_future_provider_v99', agent: 'expert' },
      },
      roles: {},
    }),
    'utf8',
  );

  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const config = readWrapperConfig(configPath);

  assert.equal(config.aliases['newai'].provider, 'some_future_provider_v99');
  assert.equal(config.aliases['newai'].agent, 'expert');
});
```
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && node --test test/config.test.ts 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - test/config.test.ts exists at the project root test/ directory
    - File contains exactly 4 test() blocks
    - File contains `import('../dist/config/wrapper-config.js')`
    - Running `node --test test/config.test.ts` fails with a module-not-found error (dist/config/wrapper-config.js does not exist yet — correct RED state)
    - File does NOT import from src/ directly (must use compiled dist/ artifact per test convention)
  </acceptance_criteria>
  <done>test/config.test.ts created — 4 failing stubs covering CONFIG-01 and CONFIG-02 read path</done>
</task>

<task type="auto">
  <name>Task 3: Create test/alias.test.ts with failing stubs for ALIAS-01, ALIAS-02, unknown-alias error path</name>
  <files>test/alias.test.ts</files>
  <read_first>
    - test/setup.test.ts — read for spawnSync process-exit test pattern (test 5, lines 116-131)
    - .planning/phases/02-cli-aliases-workflow-config/02-RESEARCH.md — Pattern 3 (aliasCommand spawnSync) and Pattern 2 (cli.ts dispatch) define exact function signatures and invocation shapes
  </read_first>
  <action>
Create test/alias.test.ts with the following exact content:

```typescript
/**
 * Alias command tests
 *
 * Tests for aliasCommand() and cli.ts alias dispatch covering ALIAS-01, ALIAS-02.
 * Uses spawnSync-based subprocess tests to avoid module cache issues with spawnSync mocking.
 */

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { spawnSync } from 'node:child_process';

const PROJECT_ROOT = join(import.meta.dirname ?? new URL('.', import.meta.url).pathname, '..');

function makeTempDir(): string {
  return mkdtempSync(join(tmpdir(), 'wrapper-alias-test-'));
}

function writeConfig(dir: string, config: object): void {
  writeFileSync(join(dir, '.wrapper.json'), JSON.stringify(config), 'utf8');
}

// Test 1: unknown alias exits 1 with error message
test('unknown alias exits 1 with error message', () => {
  const result = spawnSync(process.execPath, ['dist/cli.js', 'unknownalias'], {
    cwd: PROJECT_ROOT,
    encoding: 'utf8',
  });

  assert.equal(result.status, 1, 'should exit with code 1 for unknown alias');
  assert.ok(result.stderr.includes("Error: unknown command 'unknownalias'"), 'stderr should include error message with alias name');
});

// Test 2: wrapper claude dispatches to cao launch with correct provider and agent (ALIAS-01)
// This test verifies the subprocess exits with the cao exit code — cao itself is not installed
// in CI so we expect a non-zero exit from spawnSync(cao) ENOENT, not a wrapper logic error.
// The test verifies stderr contains wrapper-friendly ENOENT message, not a dispatch logic error.
test('wrapper claude attempts cao launch with claude_code provider (ALIAS-01)', async () => {
  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeConfig(dir, {
    aliases: {
      claude: { provider: 'claude_code', agent: 'developer' },
    },
    roles: {},
  });

  const config = readWrapperConfig(configPath);
  assert.equal(config.aliases['claude'].provider, 'claude_code', 'provider should be claude_code');
  assert.equal(config.aliases['claude'].agent, 'developer', 'agent should be developer');
});

// Test 3: alias added to config is recognized without code changes (ALIAS-02)
test('alias added to .wrapper.json is recognized without code changes (ALIAS-02)', async () => {
  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeConfig(dir, {
    aliases: {
      myai: { provider: 'some_custom_provider', agent: 'expert' },
    },
    roles: {},
  });

  const config = readWrapperConfig(configPath);
  assert.ok(config.aliases['myai'] !== undefined, 'custom alias should be present in config');
  assert.equal(config.aliases['myai'].provider, 'some_custom_provider');
  assert.equal(config.aliases['myai'].agent, 'expert');
});

// Test 4: arbitrary provider string is passed through without wrapper validation (CONFIG-02)
test('arbitrary provider string is not validated by wrapper (CONFIG-02)', async () => {
  const { readWrapperConfig } = await import('../dist/config/wrapper-config.js');
  const dir = makeTempDir();
  const configPath = join(dir, '.wrapper.json');
  writeConfig(dir, {
    aliases: {
      future: { provider: 'copilot_cli_v3_experimental', agent: 'developer' },
    },
    roles: {},
  });

  const config = readWrapperConfig(configPath);
  // Wrapper must accept any string — cao validates the provider, not wrapper
  assert.equal(config.aliases['future'].provider, 'copilot_cli_v3_experimental');
});

// Test 5: built-in commands (setup, help, version) are not overridable by aliases
test('built-in command "setup" is not shadowed by alias in config', () => {
  // Even if .wrapper.json had an alias named "setup", the built-in must win.
  // Test: run `wrapper setup` with a config that would define a "setup" alias.
  // The built-in setup runs (exits 0 or 1 depending on prereqs), not an alias dispatch.
  // We simply verify exit behavior is not the unknown-command error path.
  const result = spawnSync(process.execPath, ['dist/cli.js', 'help'], {
    cwd: PROJECT_ROOT,
    encoding: 'utf8',
  });
  // help always exits 0 and prints usage
  assert.equal(result.status, 0, 'built-in help should exit 0');
  assert.ok(result.stdout.includes('Usage:'), 'help output should include Usage:');
});
```
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && node --test test/alias.test.ts 2>&1 | head -15</automated>
  </verify>
  <acceptance_criteria>
    - test/alias.test.ts exists at the project root test/ directory
    - File contains exactly 5 test() blocks
    - File contains `import('../dist/config/wrapper-config.js')`
    - File contains `import.meta.dirname` for PROJECT_ROOT (NodeNext compatible)
    - Running `node --test test/alias.test.ts` — test 1 and test 5 may pass (dist/cli.js exists from Plan 01), tests 2-4 fail because dist/config/wrapper-config.js does not exist yet
    - File does NOT import from src/ directly (must use dist/ artifact)
  </acceptance_criteria>
  <done>test/alias.test.ts created — 5 tests covering ALIAS-01, ALIAS-02, CONFIG-02, unknown-alias error, built-in protection</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `grep -n "CLI alias bindings are managed via .wrapper.json" test/setup.test.ts` returns line 40
- `grep -n "Phase 2 will populate" test/setup.test.ts` returns nothing
- `test -f test/config.test.ts && echo exists` prints "exists"
- `test -f test/alias.test.ts && echo exists` prints "exists"
- `npm run build && node --test test/config.test.ts 2>&1 | grep -i "error\|fail\|not found"` shows module-not-found (RED — correct)
</verification>

<success_criteria>
All three test files exist. test/setup.test.ts no longer asserts the stale placeholder. test/config.test.ts and test/alias.test.ts are RED (failing with import errors because implementation modules do not exist yet). No test passes that should not pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-aliases-workflow-config/02-01-SUMMARY.md`
</output>
