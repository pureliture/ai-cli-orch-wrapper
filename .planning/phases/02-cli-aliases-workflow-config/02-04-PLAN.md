---
phase: 02-cli-aliases-workflow-config
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - ALIAS-01
  - ALIAS-02
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03

must_haves:
  truths:
    - "npm run build exits 0 with no TypeScript errors"
    - "npm run lint exits 0 with no type errors"
    - "node --test exits 0 with all tests passing"
    - "wrapper claude fires cao launch --provider claude_code --agents developer on the real machine"
    - "wrapper help lists claude, gemini, codex in the Aliases section"
    - "wrapper version reports v0.3.0"
    - "wrapper unknowncmd exits 1 with error message"
  artifacts:
    - path: "dist/cli.js"
      provides: "Built binary with alias dispatch"
      contains: "v0.3.0"
    - path: "dist/config/wrapper-config.js"
      provides: "Compiled config reader"
      exports: ["readWrapperConfig", "AliasEntry", "WrapperConfig"]
    - path: "dist/commands/alias.js"
      provides: "Compiled alias dispatcher"
      exports: ["aliasCommand"]
  key_links:
    - from: "dist/cli.js"
      to: "dist/config/wrapper-config.js"
      via: "import readWrapperConfig"
      pattern: "readWrapperConfig"
    - from: "dist/cli.js"
      to: "dist/commands/alias.js"
      via: "import aliasCommand"
      pattern: "aliasCommand"
---

<objective>
Verify the complete Phase 2 implementation on the real machine. Run the automated test suite, confirm the build is clean, and have the user validate the live alias dispatch end-to-end.

Purpose: The test suite verifies logic; this plan verifies the system works on real hardware with the actual cao binary. Mirrors the Phase 1 Plan 02 pattern.
Output: Confirmed working alias dispatch and config system. Phase 2 ready to close.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cli-aliases-workflow-config/02-CONTEXT.md
@.planning/phases/02-cli-aliases-workflow-config/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and test suite</name>
  <files></files>
  <read_first>
    - .planning/phases/02-cli-aliases-workflow-config/02-03-SUMMARY.md — confirm what was implemented in Plan 03 before running verification
  </read_first>
  <action>
Run the following commands in sequence. Each must pass before proceeding to the next.

1. Clean build:
   ```
   npm run build
   ```
   Expected: exits 0, no TypeScript errors in output.

2. Lint (type check without emit):
   ```
   npm run lint
   ```
   Expected: exits 0, no errors.

3. Full test suite:
   ```
   node --test
   ```
   Expected: exits 0, all tests pass. Specifically confirm:
   - test/setup.test.ts: 5/5 pass (test 1 now passes with new comment text)
   - test/config.test.ts: 4/4 pass
   - test/alias.test.ts: test 1 (unknown alias) and test 5 (built-in help) pass at minimum

4. Smoke-test built binary behavior:
   ```
   node dist/cli.js version
   ```
   Expected: stdout is exactly `ai-cli-orch-wrapper v0.3.0`

   ```
   node dist/cli.js help
   ```
   Expected: stdout includes "Aliases:" and lists "claude", "gemini", "codex"

   ```
   node dist/cli.js notarealcommand 2>&1; echo "exit:$?"
   ```
   Expected: stderr contains `Error: unknown command 'notarealcommand'`, exit code is 1

5. Verify dist artifacts exist:
   ```
   ls dist/config/wrapper-config.js dist/commands/alias.js dist/cli.js
   ```
   Expected: all three files present.

If any step fails, diagnose and fix before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/pureliture/ai-cli-orch-wrapper && npm run build && npm run lint && node --test 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - `node --test` exits 0
    - `node dist/cli.js version` stdout contains "v0.3.0"
    - `node dist/cli.js help` stdout contains "Aliases:" and "claude"
    - `node dist/cli.js notarealcommand 2>&1; echo $?` prints "1" as last line
    - `ls dist/config/wrapper-config.js` exits 0
    - `ls dist/commands/alias.js` exits 0
  </acceptance_criteria>
  <done>Build clean, lint clean, all tests green, binary smoke-tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke-test — live alias dispatch on real machine</name>
  <files></files>
  <action>
Pause for human verification of live alias dispatch with the actual cao binary. See how-to-verify steps below. Resume with "approved" or describe failures.
  </action>
  <verify>
    <automated>MISSING — live cao binary verification requires human; Task 1 automated suite is the automated gate</automated>
  </verify>
  <done>Human confirms all six smoke-test checks pass on real machine</done>
  <what-built>
Phase 2 complete implementation:
- wrapper claude / wrapper gemini / wrapper codex — alias dispatch to cao launch
- .wrapper.json — committed config file with aliases and roles sections
- wrapper setup — now also scaffolds .wrapper.json if missing
- wrapper help — dynamically lists aliases from .wrapper.json
- All tests green (setup, config, alias suites)
  </what-built>
  <how-to-verify>
Run these checks on your machine. You need cao installed and on PATH.

**1. Alias dispatch fires (ALIAS-01):**
```
wrapper claude --help
```
Expected: cao launches with claude_code provider. You should see cao's help or a claude session attempt. The wrapper should NOT exit with "unknown command".

**2. Help lists aliases dynamically (ALIAS-02):**
```
wrapper help
```
Expected: Output includes an "Aliases:" section listing claude, gemini, codex with their providers.

**3. Config drives dispatch without code change (ALIAS-02):**
Edit .wrapper.json, add under "aliases":
```json
"test-alias": { "provider": "codex", "agent": "developer" }
```
Then run `wrapper help`. Expected: "test-alias" appears in the Aliases section. Revert the edit after confirming.

**4. wrapper setup is idempotent for .wrapper.json:**
```
wrapper setup
```
Expected: Reports "✓ .wrapper.json: already exists". Does not overwrite.

**5. Unknown command exits correctly:**
```
wrapper xyz123 2>&1; echo "exit:$?"
```
Expected: stderr contains `Error: unknown command 'xyz123'`, last line is `1`.

**6. Version:**
```
wrapper version
```
Expected: `ai-cli-orch-wrapper v0.3.0`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Phase 2 sign-off criteria (from ROADMAP.md):
1. User can invoke `wrapper claude`, `wrapper gemini`, etc. and the correct cao invocation fires — verified by checkpoint step 1
2. User can edit config file to add or remap an alias without any code change — verified by checkpoint step 3
3. User can declare orchestrator/reviewer in config file and the wrapper respects those mappings — .wrapper.json roles section exists and is parsed (Phase 3 will consume it)
4. Config uses cao's native workflow format wherever applicable — .wrapper.json stores only provider/agent strings, no wrapper DSL invented
</verification>

<success_criteria>
Human approves all six checkpoint checks. Phase 2 requirements ALIAS-01, ALIAS-02, CONFIG-01, CONFIG-02, CONFIG-03 all satisfied and verified on live machine.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-aliases-workflow-config/02-04-SUMMARY.md`
</output>
