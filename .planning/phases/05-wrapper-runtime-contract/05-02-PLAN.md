---
phase: 05-wrapper-runtime-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/config/aco-config.ts, src/config/wrapper-config.ts, src/orchestration/workflow-config.ts, src/orchestration/workflow-runner.ts, src/cli.ts, src/commands/workflow.ts, src/commands/workflow-run.ts, src/cli-surface.ts, test/config.test.ts, test/alias.test.ts, test/canonical-command-surface.test.ts]
autonomous: true
requirements: [WRAP-01, WRAP-02]
must_haves:
  truths:
    - "Live src/ and test/ code use Aco config naming instead of WrapperConfig naming"
    - "The runtime still reads `.wrapper.json` from disk after the internal rename"
    - "CLI smoke tests prove both help output and alias dispatch still read `.wrapper.json` after the rename"
  artifacts:
    - path: "src/config/aco-config.ts"
      provides: "Canonical Aco config loader"
    - path: "test/canonical-command-surface.test.ts"
      provides: "CLI help smoke proof that `.wrapper.json` is still consumed"
    - path: "test/alias.test.ts"
      provides: "Alias-dispatch smoke proof that `aco <alias>` still resolves through `.wrapper.json`"
  key_links:
    - from: "src/cli.ts"
      to: "src/config/aco-config.ts"
      via: "readAcoConfig()"
      pattern: "readAcoConfig\\("
---

<objective>
Rename the internal config module and symbols from `Wrapper*` to `Aco*` while preserving the `.wrapper.json` disk contract and proving the user-facing CLI still reads repo-local config after the rename.

Purpose: Align internal naming with the `aco` command surface without breaking the repo-local runtime contract required by WRAP-01 and the alias-entrypoint continuity expected by WRAP-02.
Output: Refactored config module, updated imports/usages, and tests that prove both symbol cleanup and runtime continuity.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/config/wrapper-config.ts
@src/orchestration/workflow-config.ts
@src/orchestration/workflow-runner.ts
@src/cli.ts
@test/config.test.ts
@test/alias.test.ts
@test/canonical-command-surface.test.ts
@.planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
@.planning/phases/05-wrapper-runtime-contract/05-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the config module and exported symbols while keeping `.wrapper.json` as the disk contract (D-03, D-04)</name>
  <files>src/config/aco-config.ts, src/config/wrapper-config.ts</files>
  <read_first>
    - src/config/wrapper-config.ts
    - src/config/aco-config.ts
    - .planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
  </read_first>
  <action>
    Normalize the config seam around `src/config/aco-config.ts`:
    1. Rename the legacy module `src/config/wrapper-config.ts` to `src/config/aco-config.ts` if the old filename still exists in the branch being executed.
    2. Rename `WrapperConfig` to `AcoConfig`.
    3. Rename `readWrapperConfig` to `readAcoConfig`.
    4. Keep the on-disk filename constant as `.wrapper.json` by preserving the literal value `'.wrapper.json'` in the loader.
    5. If a module-level constant exists for the filename, rename the symbol to `ACO_CONFIG_FILE` but keep the value exactly `.wrapper.json`.
  </action>
  <verify>
    <automated>test -f src/config/aco-config.ts && ! test -f src/config/wrapper-config.ts && rg -n "export interface AcoConfig|readAcoConfig|\\.wrapper\\.json" src/config/aco-config.ts</automated>
  </verify>
  <done>The config seam uses `Aco*` naming while still loading `.wrapper.json` from disk.</done>
  <acceptance_criteria>
    - `src/config/aco-config.ts` exists
    - `src/config/wrapper-config.ts` does not exist
    - `src/config/aco-config.ts` contains `export interface AcoConfig`
    - `src/config/aco-config.ts` contains `readAcoConfig`
    - `src/config/aco-config.ts` contains `.wrapper.json`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update every live consumer to the Aco config seam and remove stale WrapperConfig references</name>
  <files>src/orchestration/workflow-config.ts, src/orchestration/workflow-runner.ts, src/cli.ts, src/commands/workflow.ts, src/commands/workflow-run.ts, src/cli-surface.ts</files>
  <read_first>
    - src/orchestration/workflow-config.ts
    - src/orchestration/workflow-runner.ts
    - src/cli.ts
    - src/commands/workflow.ts
    - src/commands/workflow-run.ts
    - src/cli-surface.ts
    - src/config/aco-config.ts
  </read_first>
  <action>
    Update every live import and type usage from the old config seam to the new one:
    - Replace `wrapper-config.js` imports with `aco-config.js`.
    - Replace `WrapperConfig` type references with `AcoConfig`.
    - Replace `readWrapperConfig()` call sites with `readAcoConfig()`.
    - Rename local variables from `wrapperConfig` to `acoConfig` where the variable represents the parsed config object.

    After the rename, clean up stale naming only in live `src/` and `test/` paths. The repo-wide negative search must specifically remove these stale identifiers from executable code:
    - `wrapper-config`
    - `WrapperConfig`
    - `readWrapperConfig`
  </action>
  <verify>
    <automated>npm run build && ! rg -n "wrapper-config|WrapperConfig|readWrapperConfig" src test</automated>
  </verify>
  <done>All live code compiles through the Aco config seam and no stale config identifiers remain in `src/` or `test/`.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-config.ts` imports from `../config/aco-config.js`
    - `src/cli.ts` calls `readAcoConfig()`
    - `src/` and `test/` contain no `wrapper-config`, `WrapperConfig`, or `readWrapperConfig` matches
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lock config behavior with unit coverage plus CLI smokes for help and alias dispatch</name>
  <files>test/config.test.ts, test/alias.test.ts, test/canonical-command-surface.test.ts</files>
  <read_first>
    - test/config.test.ts
    - test/alias.test.ts
    - test/canonical-command-surface.test.ts
    - src/config/aco-config.ts
    - src/cli.ts
  </read_first>
  <action>
    Update `test/config.test.ts` so it still proves `readAcoConfig()` handles:
    - valid `.wrapper.json`
    - missing `.wrapper.json`
    - malformed `.wrapper.json`

    Add a help-path CLI smoke to `test/canonical-command-surface.test.ts` that proves the renamed config seam still feeds the user path:
    1. Create a temp directory with a `.wrapper.json` file whose `aliases` include `claude`.
    2. Run `node dist/cli.js help` in that directory.
    3. Assert exit code `0`.
    4. Assert help output includes the alias name `claude`.
    5. Assert help output still uses `aco` branding, not `wrapper`.

    Strengthen `test/alias.test.ts` with an alias-dispatch smoke:
    1. Create a temp directory with `.wrapper.json` containing alias `claude`.
    2. Run `node dist/cli.js claude` in that directory.
    3. Assert the CLI does not fail with `unknown command 'claude'`.
    4. It is acceptable for the subprocess to exit non-zero because `cao` is unavailable in CI, but the failure must prove alias dispatch happened rather than config lookup failing.
  </action>
  <verify>
    <automated>npm run build && node --test test/config.test.ts test/alias.test.ts test/canonical-command-surface.test.ts</automated>
  </verify>
  <done>Unit tests plus help and alias CLI smoke tests prove the renamed config seam still reads `.wrapper.json` correctly.</done>
  <acceptance_criteria>
    - `test/config.test.ts` imports `readAcoConfig` from `../dist/config/aco-config.js` or `../src/config/aco-config.js` according to existing test style
    - `test/canonical-command-surface.test.ts` contains a help-path smoke test that asserts `claude` appears in help output
    - `test/alias.test.ts` contains an alias-dispatch smoke test that proves `aco claude` is recognized through `.wrapper.json`
    - `node --test test/config.test.ts test/alias.test.ts test/canonical-command-surface.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the build, then prove both naming cleanup and user-path continuity with targeted tests rather than a build-only check.
</verification>

<success_criteria>
1. `WrapperConfig`, `readWrapperConfig`, and `wrapper-config` references are removed from live `src/` and `test/` code.
2. `src/config/aco-config.ts` remains the canonical config loader and still targets `.wrapper.json`.
3. `node dist/cli.js help` in a temp repo with `.wrapper.json` succeeds and shows alias data from that file.
4. `node dist/cli.js claude` in a temp repo with `.wrapper.json` proves alias dispatch still resolves through config lookup.
5. `npm run build` and `node --test test/config.test.ts test/alias.test.ts test/canonical-command-surface.test.ts` exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-02-SUMMARY.md`
</output>
