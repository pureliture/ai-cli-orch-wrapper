---
phase: 05-wrapper-runtime-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/config/aco-config.ts, src/config/wrapper-config.ts, src/orchestration/workflow-config.ts, src/orchestration/workflow-runner.ts, src/cli.ts, src/commands/workflow.ts, src/commands/workflow-run.ts, src/cli-surface.ts, test/config.test.ts]
autonomous: true
requirements: [WRAP-01]
must_haves:
  truths:
    - "Internal symbols use Aco prefix exclusively"
    - "CLI still reads .wrapper.json from disk"
  artifacts:
    - path: "src/config/aco-config.ts"
      provides: "New configuration module"
    - path: "src/cli.ts"
      contains: "readAcoConfig"
  key_links:
    - from: "src/config/aco-config.ts"
      to: ".wrapper.json"
      via: "fs read"
      pattern: "\\.wrapper\\.json"
---

<objective>
Rename internal `WrapperConfig` symbols and the config loader file to `AcoConfig` and `aco-config.ts` while maintaining the `.wrapper.json` on-disk contract (D-03, D-04).

Purpose: Align internal naming with the `aco` branding while preserving backward compatibility with existing repo configurations.
Output: Refactored config module and updated usages across the codebase.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/config/wrapper-config.ts
@src/orchestration/workflow-config.ts
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Config symbols and file (D-03, D-04)</name>
  <files>src/config/aco-config.ts, src/config/wrapper-config.ts</files>
  <read_first>
    - src/config/wrapper-config.ts
  </read_first>
  <action>
    1. Rename `src/config/wrapper-config.ts` to `src/config/aco-config.ts`.
    2. Perform the following symbol renames inside the file:
       - `WrapperConfig` -> `AcoConfig`
       - `readWrapperConfig` -> `readAcoConfig`
    3. Ensure that `readAcoConfig` still targets `.wrapper.json` as the file to load on disk (D-04).
    4. Update the `WRAPPER_CONFIG_FILE = '.wrapper.json'` constant to be named `ACO_CONFIG_FILE = '.wrapper.json'` for internal consistency if applicable, but keep the value as `.wrapper.json`.
  </action>
  <verify>
    <automated>test -f src/config/aco-config.ts && ! test -f src/config/wrapper-config.ts && grep "export interface AcoConfig" src/config/aco-config.ts && grep ".wrapper.json" src/config/aco-config.ts</automated>
  </verify>
  <done>Config file and symbols renamed; disk contract preserved as `.wrapper.json`.</done>
  <acceptance_criteria>
    - `src/config/aco-config.ts` exists
    - `src/config/wrapper-config.ts` does NOT exist
    - `src/config/aco-config.ts` contains `export interface AcoConfig`
    - `src/config/aco-config.ts` contains `'.wrapper.json'`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update Config imports and usages across src/</name>
  <files>src/orchestration/workflow-config.ts, src/orchestration/workflow-runner.ts, src/cli.ts, src/commands/workflow.ts, src/commands/workflow-run.ts, src/cli-surface.ts</files>
  <read_first>
    - src/orchestration/workflow-config.ts
    - src/cli.ts
  </read_first>
  <action>
    Update all files that imported from `../config/wrapper-config.js` to import from `../config/aco-config.js` and use the renamed symbols:
    - Replace `import { ...WrapperConfig... } from '../config/wrapper-config.js'` with `import { ...AcoConfig... } from '../config/aco-config.js'`.
    - Rename variables and type references from `wrapperConfig` to `acoConfig` (or similar) where appropriate for clarity.
    - Specifically update `src/orchestration/workflow-config.ts` to use `AcoConfig`.
    - Update `src/cli.ts` to use `readAcoConfig`.
    - Update `src/cli-surface.ts` if it references the config file name or symbols.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Codebase compiles successfully with renamed config symbols.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-config.ts` imports from `../config/aco-config.js`
    - `src/cli.ts` calls `readAcoConfig()`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update Config tests</name>
  <files>test/config.test.ts</files>
  <read_first>
    - test/config.test.ts
  </read_first>
  <action>
    Update `test/config.test.ts` to reflect the rename:
    - Update imports to use `AcoConfig` and `readAcoConfig` from `../src/config/aco-config.js`.
    - Update test descriptions and variables to use `aco` branding where appropriate.
    - Ensure tests still verify that `.wrapper.json` is the file being read.
  </action>
  <verify>
    <automated>npm test test/config.test.ts</automated>
  </verify>
  <done>Config tests pass with renamed symbols.</done>
  <acceptance_criteria>
    - `test/config.test.ts` imports from `../src/config/aco-config.js`
    - `npm test test/config.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npm run build` and `npm test` to ensure the refactoring didn't break existing functionality.
</verification>

<success_criteria>
1. `WrapperConfig` and `readWrapperConfig` are renamed to `AcoConfig` and `readAcoConfig`.
2. The config loader still reads `.wrapper.json` from disk.
3. The codebase builds successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-02-SUMMARY.md`
</output>
