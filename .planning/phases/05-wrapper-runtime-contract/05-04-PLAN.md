---
phase: 05-wrapper-runtime-contract
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified: [src/cli.ts, src/commands/setup.ts, src/orchestration/workflow-runner.ts, test/canonical-command-surface.test.ts, test/setup.test.ts, test/workflow-cli.test.ts, test/workflow-runner.test.ts]
autonomous: true
requirements: [CMD-03, WRAP-01]
must_haves:
  truths:
    - "Alias conflicts are rejected for every reserved built-in command before dispatch"
    - "`aco setup` creates `.wrapper.json` on first run and preserves an existing `.wrapper.json` on rerun"
    - "`aco setup` output and generated comments use `aco` branding"
    - "`ACO_CAO_BASE_URL` is preferred and `WRAPPER_CAO_BASE_URL` remains a fallback"
  artifacts:
    - path: "src/cli.ts"
      contains: "BUILTIN_COMMANDS"
    - path: "src/commands/setup.ts"
      contains: "Managed by aco setup"
    - path: "src/orchestration/workflow-runner.ts"
      contains: "ACO_CAO_BASE_URL"
  key_links:
    - from: "src/orchestration/workflow-runner.ts"
      to: "CAO base URL resolution"
      via: "ACO_CAO_BASE_URL || WRAPPER_CAO_BASE_URL"
      pattern: "ACO_CAO_BASE_URL|WRAPPER_CAO_BASE_URL"
---

<objective>
Finish the runtime contract by enforcing alias conflicts before dispatch, locking `aco setup` behavior around the `.wrapper.json` contract, and explicitly testing the `ACO_CAO_BASE_URL` preference with `WRAPPER_CAO_BASE_URL` fallback.

Purpose: Prove the final Phase 05 user-facing behaviors for CMD-03 and WRAP-01 rather than relying on build-only or single-case verification.
Output: Hardened CLI/runtime behavior with table-driven alias-conflict tests, setup lifecycle tests, and explicit env-var fallback coverage.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cli.ts
@src/commands/setup.ts
@src/orchestration/workflow-runner.ts
@test/canonical-command-surface.test.ts
@test/setup.test.ts
@test/workflow-cli.test.ts
@test/workflow-runner.test.ts
@.planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
@.planning/phases/05-wrapper-runtime-contract/05-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce alias conflicts for every reserved built-in command before dispatch (CMD-03, D-06)</name>
  <files>src/cli.ts, test/canonical-command-surface.test.ts, test/workflow-cli.test.ts</files>
  <read_first>
    - src/cli.ts
    - src/cli-surface.ts
    - test/canonical-command-surface.test.ts
    - test/workflow-cli.test.ts
    - src/config/aco-config.ts
  </read_first>
  <action>
    In `src/cli.ts`, centralize the reserved command list as:
    - `setup`
    - `help`
    - `version`
    - `workflow`
    - `workflow-run`
    - `alias`

    Immediately after `readAcoConfig()` returns, iterate over `Object.keys(config.aliases)` and reject any alias that matches one of those reserved names with this exact error shape:
    `Error: alias '${alias}' in .wrapper.json conflicts with a built-in command.`

    The conflict check must happen before legacy-command remediation, help/version rendering, workflow dispatch, or alias dispatch.

    Update tests so conflict coverage is table-driven across the reserved command set instead of proving only `setup`. Reuse `test/canonical-command-surface.test.ts` and `test/workflow-cli.test.ts` rather than creating a one-off test file.
  </action>
  <verify>
    <automated>npm run build && node --test test/canonical-command-surface.test.ts test/workflow-cli.test.ts</automated>
  </verify>
  <done>The CLI rejects reserved alias names consistently before any command dispatch occurs.</done>
  <acceptance_criteria>
    - `src/cli.ts` contains `BUILTIN_COMMANDS` with `setup`, `help`, `version`, `workflow`, `workflow-run`, and `alias`
    - the alias-conflict check happens before command dispatch in `src/cli.ts`
    - `test/canonical-command-surface.test.ts` or `test/workflow-cli.test.ts` iterates over multiple reserved commands rather than only `setup`
    - `node --test test/canonical-command-surface.test.ts test/workflow-cli.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lock `aco setup` first-run creation, rerun preservation, and branded comments (WRAP-01, D-02)</name>
  <files>src/commands/setup.ts, test/setup.test.ts</files>
  <read_first>
    - src/commands/setup.ts
    - test/setup.test.ts
    - .planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
  </read_first>
  <action>
    In `src/commands/setup.ts`, keep the on-disk contract at `.wrapper.json` and the generated comment string at `Managed by aco setup`.

    Extend `test/setup.test.ts` with explicit first-run and rerun behavior checks:
    1. First run in a temp repo creates `.wrapper.json`
    2. The created file contains the comment `Managed by aco setup`
    3. Second run leaves an existing `.wrapper.json` unchanged byte-for-byte
    4. Second run reports `✓ .wrapper.json: already exists`

    Keep the existing prerequisite failure coverage and tmux config assertions.
  </action>
  <verify>
    <automated>npm run build && node --test test/setup.test.ts</automated>
  </verify>
  <done>`aco setup` is proven to create `.wrapper.json` once, preserve it on rerun, and keep the `aco` branding in comments and logs.</done>
  <acceptance_criteria>
    - `src/commands/setup.ts` contains `Managed by aco setup`
    - `src/commands/setup.ts` still writes `.wrapper.json`
    - `test/setup.test.ts` asserts `.wrapper.json` is created on first run
    - `test/setup.test.ts` asserts rerun preserves the existing `.wrapper.json` contents
    - `node --test test/setup.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Keep env-var fallback support and explicitly prove preference and fallback behavior</name>
  <files>src/orchestration/workflow-runner.ts, test/workflow-runner.test.ts, test/workflow-cli.test.ts</files>
  <read_first>
    - src/orchestration/workflow-runner.ts
    - test/workflow-runner.test.ts
    - test/workflow-cli.test.ts
  </read_first>
  <action>
    Keep the runtime base-URL resolution in `src/orchestration/workflow-runner.ts` with this exact precedence:
    1. `process.env.ACO_CAO_BASE_URL`
    2. `process.env.WRAPPER_CAO_BASE_URL`
    3. `undefined` when neither variable is set

    If the current code structure makes that precedence hard to test, extract a small helper inside `src/orchestration/workflow-runner.ts` so `test/workflow-runner.test.ts` can assert the exact three cases above without needing a live CAO server.

    Preserve the existing workflow CLI compatibility test that uses `WRAPPER_CAO_BASE_URL`, and add a direct test that when both vars are set the `ACO_` value wins.
  </action>
  <verify>
    <automated>npm run build && node --test test/workflow-runner.test.ts test/workflow-cli.test.ts</automated>
  </verify>
  <done>Env-var compatibility remains in place and tests explicitly prove `ACO_` preference plus `WRAPPER_` fallback.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-runner.ts` contains `process.env.ACO_CAO_BASE_URL`
    - `src/orchestration/workflow-runner.ts` contains `process.env.WRAPPER_CAO_BASE_URL`
    - `test/workflow-runner.test.ts` asserts `ACO_CAO_BASE_URL` wins when both vars are set
    - `test/workflow-runner.test.ts` asserts `WRAPPER_CAO_BASE_URL` is used when `ACO_CAO_BASE_URL` is absent
    - `node --test test/workflow-runner.test.ts test/workflow-cli.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Use targeted runtime tests to prove alias precedence, setup lifecycle behavior, and env-var resolution. Do not stop at build success or a single example alias.
</verification>

<success_criteria>
1. Reserved built-in commands cannot be shadowed by aliases in `.wrapper.json`.
2. `aco setup` creates `.wrapper.json` on first run and preserves the existing file on rerun.
3. `aco setup` output and generated comments use `aco` branding.
4. `ACO_CAO_BASE_URL` is preferred while `WRAPPER_CAO_BASE_URL` still works as fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-04-SUMMARY.md`
</output>
