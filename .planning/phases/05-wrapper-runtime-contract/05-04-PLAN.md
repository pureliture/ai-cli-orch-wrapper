---
phase: 05-wrapper-runtime-contract
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified: [src/cli.ts, src/commands/setup.ts, src/orchestration/workflow-runner.ts, test/canonical-command-surface.test.ts]
autonomous: true
requirements: [CMD-03, WRAP-01]
must_haves:
  truths:
    - "CLI exits on alias conflict with built-in commands"
    - "Setup logs and file headers use aco branding"
    - "ACO_CAO_BASE_URL env var is supported with fallback"
  artifacts:
    - path: "src/cli.ts"
      contains: "BUILTIN_COMMANDS"
    - path: "src/commands/setup.ts"
      contains: "Managed by aco"
  key_links:
    - from: "src/orchestration/workflow-runner.ts"
      to: "CAO Server"
      via: "ACO_CAO_BASE_URL"
      pattern: "ACO_CAO_BASE_URL"
---

<objective>
Implement runtime protection against alias conflicts, align `aco setup` output with new branding, and provide environment variable fallbacks for a smooth transition (CMD-03, D-06, D-02).

Purpose: Ensure the CLI remains robust as it consolidates to the `aco` command while providing safety guards and backward-compatible environment support.
Output: Updated CLI entry point with conflict checks, branded setup command, and environment variable fallbacks.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cli.ts
@src/commands/setup.ts
@src/orchestration/workflow-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Alias Conflict Protection (CMD-03, D-06, Review)</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/cli.ts
    - src/config/aco-config.ts
  </read_first>
  <action>
    In `src/cli.ts`, implement a check to ensure that user-defined aliases in `.wrapper.json` do not conflict with built-in `aco` subcommands:
    1. Define `const BUILTIN_COMMANDS = ['setup', 'help', 'version', 'workflow', 'workflow-run', 'alias'];`.
    2. After loading the config with `readAcoConfig()`, iterate over `Object.keys(config.aliases)`.
    3. If any alias name is present in `BUILTIN_COMMANDS`, print an error message: `Error: alias '${alias}' in .wrapper.json conflicts with a built-in command.` and exit with code 1.
    4. Ensure this check happens *before* any command dispatch logic.
  </action>
  <verify>
    <automated>grep "const BUILTIN_COMMANDS" src/cli.ts && grep "conflicts with a built-in command" src/cli.ts</automated>
  </verify>
  <done>CLI prevents execution if an alias conflicts with a reserved subcommand.</done>
  <acceptance_criteria>
    - `src/cli.ts` contains `BUILTIN_COMMANDS` including 'alias'
    - `src/cli.ts` exits with error on alias conflict
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Align setup branding and file headers (D-02, WRAP-01)</name>
  <files>src/commands/setup.ts</files>
  <read_first>
    - src/commands/setup.ts
  </read_first>
  <action>
    Update `src/commands/setup.ts` to use `aco` branding in all user-facing output:
    1. Update `console.log` statements to identify the tool as `aco` (e.g., `✓ .wrapper.json: already exists` is fine, but any generic tool mention should be `aco`).
    2. Update the header template for generated `.wrapper.json` files to: `# Managed by aco setup — do not edit manually.` (or similar).
    3. Ensure that the generated config still uses the `.wrapper.json` filename on disk.
  </action>
  <verify>
    <automated>grep "Managed by aco" src/commands/setup.ts</automated>
  </verify>
  <done>Setup command and generated file headers use `aco` branding.</done>
  <acceptance_criteria>
    - `src/commands/setup.ts` contains branded file header "Managed by aco"
    - `src/commands/setup.ts` still references `.wrapper.json` for disk operations
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement Env Var Fallbacks and update tests (Review)</name>
  <files>src/orchestration/workflow-runner.ts, test/canonical-command-surface.test.ts</files>
  <read_first>
    - src/orchestration/workflow-runner.ts
    - test/canonical-command-surface.test.ts
  </read_first>
  <action>
    1. In `src/orchestration/workflow-runner.ts`, update the `CaoHttpClient` instantiation to use a fallback:
       `const client = options?.client ?? new CaoHttpClient(process.env.ACO_CAO_BASE_URL || process.env.WRAPPER_CAO_BASE_URL);`
    2. Update `test/canonical-command-surface.test.ts` to add a test case verifying the alias conflict check:
       - Mock or create a temporary `.wrapper.json` with a conflicting alias (e.g., `"setup": { ... }`).
       - Run the CLI and assert it exits with the expected error message and code 1.
  </action>
  <verify>
    <automated>npm test test/canonical-command-surface.test.ts</automated>
  </verify>
  <done>Environment variable fallbacks implemented and conflict check verified by tests.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-runner.ts` contains `process.env.ACO_CAO_BASE_URL || process.env.WRAPPER_CAO_BASE_URL`
    - `test/canonical-command-surface.test.ts` includes a test for alias conflicts
    - `npm test test/canonical-command-surface.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verify all 4 plans are complete and that the `aco` command surface correctly handles the existing `.wrapper*` runtime contract.
</verification>

<success_criteria>
1. User-defined aliases cannot override built-in `aco` commands.
2. `aco setup` produces correctly branded file headers.
3. `ACO_CAO_BASE_URL` is preferred over `WRAPPER_CAO_BASE_URL`.
4. All Phase 05 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-04-SUMMARY.md`
</output>
