---
phase: 05-wrapper-runtime-contract
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified: [src/orchestration/artifacts.ts, src/orchestration/workflow-runner.ts, src/commands/workflow-run.ts, test/artifacts.test.ts, test/workflow-runner.test.ts, test/workflow-cli.test.ts]
autonomous: true
requirements: [WRAP-02]
must_haves:
  truths:
    - "Artifact helper symbols use the Aco prefix"
    - "Workflow artifacts still land under `.wrapper/workflows/`"
    - "A named-workflow CLI smoke proves the `.wrapper/workflows/` contract survives the internal rename"
  artifacts:
    - path: "src/orchestration/artifacts.ts"
      contains: "createAcoWorkflowRunArtifacts"
    - path: "test/workflow-cli.test.ts"
      provides: "CLI proof that workflow runs still create `.wrapper/workflows/...` artifacts"
  key_links:
    - from: "src/orchestration/workflow-runner.ts"
      to: "src/orchestration/artifacts.ts"
      via: "createAcoWorkflowRunArtifacts()"
      pattern: "createAcoWorkflowRunArtifacts\\("
---

<objective>
Rename the internal artifact helpers and their immediate workflow-runner consumers to `Aco` branding while keeping the on-disk artifact contract rooted at `.wrapper/workflows/`.

Purpose: Align the orchestration internals with the `aco` command surface without changing where workflow runs persist their repo-local artifacts.
Output: Refactored artifact helpers, updated immediate consumers, and tests that prove both helper-level and CLI-level artifact continuity.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/orchestration/artifacts.ts
@src/orchestration/workflow-runner.ts
@src/commands/workflow-run.ts
@test/artifacts.test.ts
@test/workflow-runner.test.ts
@test/workflow-cli.test.ts
@.planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
@.planning/phases/05-wrapper-runtime-contract/05-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename artifact helper types and constructors while preserving the `.wrapper/workflows` root (D-01, D-03)</name>
  <files>src/orchestration/artifacts.ts</files>
  <read_first>
    - src/orchestration/artifacts.ts
    - .planning/phases/05-wrapper-runtime-contract/05-CONTEXT.md
  </read_first>
  <action>
    In `src/orchestration/artifacts.ts`, perform only the artifact-helper rename work:
    - Rename `WorkflowRunArtifacts` to `AcoWorkflowRunArtifacts`
    - Rename `IterationArtifacts` to `AcoIterationArtifacts`
    - Rename `createWorkflowRunArtifacts` to `createAcoWorkflowRunArtifacts`
    - Rename `createIterationArtifacts` to `createAcoIterationArtifacts`

    Keep the artifact root literal exactly `'.wrapper/workflows'`. Do not rename the directory contract or broaden this plan into unrelated orchestration surfaces.
  </action>
  <verify>
    <automated>rg -n "export interface AcoWorkflowRunArtifacts|export interface AcoIterationArtifacts|createAcoWorkflowRunArtifacts|createAcoIterationArtifacts|\\.wrapper/workflows" src/orchestration/artifacts.ts</automated>
  </verify>
  <done>Artifact helper symbols use `Aco` naming and still point at `.wrapper/workflows`.</done>
  <acceptance_criteria>
    - `src/orchestration/artifacts.ts` contains `export interface AcoWorkflowRunArtifacts`
    - `src/orchestration/artifacts.ts` contains `export interface AcoIterationArtifacts`
    - `src/orchestration/artifacts.ts` contains `createAcoWorkflowRunArtifacts`
    - `src/orchestration/artifacts.ts` contains `createAcoIterationArtifacts`
    - `src/orchestration/artifacts.ts` contains `.wrapper/workflows`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update the immediate workflow consumers to the renamed artifact helpers</name>
  <files>src/orchestration/workflow-runner.ts, src/commands/workflow-run.ts</files>
  <read_first>
    - src/orchestration/workflow-runner.ts
    - src/commands/workflow-run.ts
    - src/orchestration/artifacts.ts
  </read_first>
  <action>
    Update only the immediate consumers that instantiate or type-check workflow artifacts:
    - In `src/orchestration/workflow-runner.ts`, import and call `createAcoWorkflowRunArtifacts` and `createAcoIterationArtifacts`
    - Update any type annotations in `src/orchestration/workflow-runner.ts` to `AcoWorkflowRunArtifacts` or `AcoIterationArtifacts`
    - In `src/commands/workflow-run.ts`, update the artifact helper imports or type references to the new names

    Keep this plan boundary narrow: artifact helpers plus their immediate runner/command consumers. Do not attempt a repo-wide orchestration rename here.
  </action>
  <verify>
    <automated>npm run build && ! rg -n "\\bWorkflowRunArtifacts\\b|\\bIterationArtifacts\\b|\\bcreateWorkflowRunArtifacts\\b|\\bcreateIterationArtifacts\\b" src test && rg -n "createAcoWorkflowRunArtifacts|createAcoIterationArtifacts" src/orchestration/workflow-runner.ts src/commands/workflow-run.ts</automated>
  </verify>
  <done>The workflow runner and workflow-run command compile against the renamed artifact helpers.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-runner.ts` imports `createAcoWorkflowRunArtifacts`
    - `src/orchestration/workflow-runner.ts` imports `createAcoIterationArtifacts`
    - `src/commands/workflow-run.ts` uses the renamed artifact helpers or types
    - `src/` and `test/` contain no standalone `WorkflowRunArtifacts`, `IterationArtifacts`, `createWorkflowRunArtifacts`, or `createIterationArtifacts` matches
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove artifact continuity with helper tests and a named-workflow CLI smoke (WRAP-02)</name>
  <files>test/artifacts.test.ts, test/workflow-runner.test.ts, test/workflow-cli.test.ts</files>
  <read_first>
    - test/artifacts.test.ts
    - test/workflow-runner.test.ts
    - test/workflow-cli.test.ts
    - src/orchestration/artifacts.ts
    - src/orchestration/workflow-runner.ts
  </read_first>
  <action>
    Update the existing tests to prove both helper-level and CLI-level behavior:
    - In `test/artifacts.test.ts`, keep asserting that `createAcoWorkflowRunArtifacts()` creates paths rooted under `.wrapper/workflows`
    - In `test/workflow-runner.test.ts`, update imports/usages to the renamed artifact helpers and keep run-directory assertions under `.wrapper/workflows`
    - In `test/workflow-cli.test.ts`, add or strengthen a named-workflow CLI smoke:
      1. Create a temp repo with `.wrapper.json` defining workflow `plan-review`
      2. Run `node dist/cli.js workflow plan-review --max-iterations 1`
      3. Force the CAO call to fail fast with `WRAPPER_CAO_BASE_URL=http://127.0.0.1:9`
      4. Assert the command exits non-zero because CAO is unavailable
      5. Assert a run directory still appears under `.wrapper/workflows/plan-review/runs/`
      6. Assert that run directory contains `run.json`
  </action>
  <verify>
    <automated>npm run build && node --test test/artifacts.test.ts test/workflow-runner.test.ts test/workflow-cli.test.ts</automated>
  </verify>
  <done>Tests prove that internal helper renames did not change the `.wrapper/workflows` artifact contract.</done>
  <acceptance_criteria>
    - `test/artifacts.test.ts` imports `createAcoWorkflowRunArtifacts`
    - `test/workflow-runner.test.ts` passes with renamed helper imports
    - `test/workflow-cli.test.ts` asserts the run directory is under `.wrapper/workflows/plan-review/runs/`
    - `node --test test/artifacts.test.ts test/workflow-runner.test.ts test/workflow-cli.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Use one helper-level test pass and one CLI-level smoke pass so the plan proves `.wrapper/workflows` continuity, not just symbol renames.
</verification>

<success_criteria>
1. Artifact helper types and constructors use the `Aco` prefix.
2. Immediate workflow consumers compile against the renamed artifact helpers.
3. Unit and integration tests still assert `.wrapper/workflows` artifact paths.
4. A named-workflow CLI smoke demonstrates that workflow artifacts still land under `.wrapper/workflows/...`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-03-SUMMARY.md`
</output>
