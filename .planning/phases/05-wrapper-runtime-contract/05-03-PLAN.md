---
phase: 05-wrapper-runtime-contract
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified: [src/orchestration/artifacts.ts, src/orchestration/workflow-runner.ts, src/commands/workflow-run.ts, test/artifacts.test.ts, test/workflow-runner.test.ts]
autonomous: true
requirements: [WRAP-02]
must_haves:
  truths:
    - "Artifact symbols and creators use Aco prefix"
    - "Workflow artifacts continue to land in .wrapper/workflows/"
  artifacts:
    - path: "src/orchestration/artifacts.ts"
      contains: "createAcoWorkflowRunArtifacts"
    - path: "src/orchestration/workflow-runner.ts"
      contains: "createAcoWorkflowRunArtifacts"
  key_links:
    - from: "src/orchestration/artifacts.ts"
      to: ".wrapper/workflows"
      via: "path join"
      pattern: "\\.wrapper/workflows"
---

<objective>
Rename internal `WorkflowRunArtifacts` and `IterationArtifacts` symbols and their creator functions to use `Aco` prefix while maintaining the `.wrapper/` on-disk contract (D-03).

Purpose: Align internal orchestration code with the `aco` branding while ensuring workflow artifacts continue to land in the established v1.0 directory structure.
Output: Refactored artifacts module and updated orchestration logic.
</objective>

<execution_context>
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/ai-cli-orch-wrapper/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/orchestration/artifacts.ts
@src/orchestration/workflow-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Artifact symbols and creators (D-03, Review)</name>
  <files>src/orchestration/artifacts.ts</files>
  <read_first>
    - src/orchestration/artifacts.ts
  </read_first>
  <action>
    In `src/orchestration/artifacts.ts`, perform the following symbol renames:
    - `WorkflowRunArtifacts` -> `AcoWorkflowRunArtifacts`
    - `IterationArtifacts` -> `AcoIterationArtifacts`
    - `createWorkflowRunArtifacts` -> `createAcoWorkflowRunArtifacts`
    - `createIterationArtifacts` -> `createAcoIterationArtifacts`

    CRITICAL: Keep `WORKFLOW_ARTIFACT_ROOT = '.wrapper/workflows'` UNCHANGED to preserve the on-disk contract (D-01).
  </action>
  <verify>
    <automated>grep "export interface AcoWorkflowRunArtifacts" src/orchestration/artifacts.ts && grep "export function createAcoWorkflowRunArtifacts" src/orchestration/artifacts.ts && grep "'.wrapper/workflows'" src/orchestration/artifacts.ts</automated>
  </verify>
  <done>Artifact symbols and creators renamed; `.wrapper/workflows` path preserved.</done>
  <acceptance_criteria>
    - `src/orchestration/artifacts.ts` contains `export interface AcoWorkflowRunArtifacts`
    - `src/orchestration/artifacts.ts` contains `export function createAcoWorkflowRunArtifacts`
    - `src/orchestration/artifacts.ts` contains `'.wrapper/workflows'`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update Artifact usages in orchestration and commands</name>
  <files>src/orchestration/workflow-runner.ts, src/commands/workflow-run.ts</files>
  <read_first>
    - src/orchestration/workflow-runner.ts
    - src/commands/workflow-run.ts
  </read_first>
  <action>
    Update all usages of the renamed artifact symbols and functions:
    - In `src/orchestration/workflow-runner.ts`, update imports from `./artifacts.js` and call sites for `createAcoWorkflowRunArtifacts` and `createAcoIterationArtifacts`.
    - In `src/commands/workflow-run.ts`, update imports and usages similarly.
    - Ensure all variables referencing these types are updated (e.g., `runArtifacts` remains fine, but the type annotation should be `AcoWorkflowRunArtifacts`).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Orchestration code and workflow command updated and compiling.</done>
  <acceptance_criteria>
    - `src/orchestration/workflow-runner.ts` imports `createAcoWorkflowRunArtifacts`
    - `src/commands/workflow-run.ts` imports `createAcoWorkflowRunArtifacts`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update Artifact and Runner tests</name>
  <files>test/artifacts.test.ts, test/workflow-runner.test.ts</files>
  <read_first>
    - test/artifacts.test.ts
    - test/workflow-runner.test.ts
  </read_first>
  <action>
    Update tests to reflect the renamed symbols:
    - In `test/artifacts.test.ts`, update imports and verify that artifacts still land in `.wrapper/workflows/` (WRAP-02).
    - In `test/workflow-runner.test.ts`, update imports and usages of renamed artifact functions.
    - Update test descriptions to use `aco` branding.
  </action>
  <verify>
    <automated>npm test test/artifacts.test.ts test/workflow-runner.test.ts</automated>
  </verify>
  <done>Tests updated and passing for artifact and runner modules.</done>
  <acceptance_criteria>
    - `test/artifacts.test.ts` imports `createAcoWorkflowRunArtifacts`
    - `npm test test/artifacts.test.ts test/workflow-runner.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npm run build` and the specified tests to ensure artifact generation still works and targets `.wrapper/`.
</verification>

<success_criteria>
1. `WorkflowRunArtifacts` and `IterationArtifacts` symbols (and creators) use the `Aco` prefix.
2. Artifacts are still created under `.wrapper/workflows/`.
3. Codebase builds and artifact tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wrapper-runtime-contract/05-03-SUMMARY.md`
</output>
