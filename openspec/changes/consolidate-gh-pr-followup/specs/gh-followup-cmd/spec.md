## REMOVED Requirements

### Requirement: /gh-followup command creates a post-review followup issue
**Reason**: Replaced by the unified `/gh-pr-followup` command which handles both immediate resolutions and issue deferrals intelligently.
**Migration**: Use `/gh-pr-followup` instead to manage review comments.
