{
  "name": "ai-cli-orch-wrapper",
  "version": "0.4.0",
  "description": "Claude Code slash-command pack for Gemini CLI and GitHub Copilot CLI — installable npm workspace.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/wrapper && npm run build --workspace=packages/installer",
    "test": "npm test --workspace=packages/wrapper",
    "test:smoke": "npm run test:smoke --workspace=packages/wrapper --if-present",
    "test:fixtures": "npx ts-node test/fixtures/harness.ts"
  },
  "keywords": [
    "claude-code",
    "gemini-cli",
    "copilot-cli",
    "slash-commands"
  ],
  "license": "MIT"
}
